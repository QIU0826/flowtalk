# FlowTalk — 产品需求文档

## 产品定位

不只是语音转文字，更是"你说人话，它出成文稿"的AI输入工具。

**核心差异化**：AI语义改写层。现有产品（搜狗、讯飞、豆包、微信输入法）只做语音识别这一步，不做AI改写。用户拿到的原始转录充满语气词、重复和断句混乱，手动修改的时间有时比直接打字还长。FlowTalk 在识别之后加了一层 AI 改写——去噪、润色、分段、场景匹配格式，让语音输入直接产出"可用的文字"。

**场景边界**：FlowTalk 的核心场景是**单人语音输入**，不是多人会议录制。会议模式定位为"一个人会后口述整理纪要"，而非替代飞书妙记、通义听悟这类多人会议转录工具。

## 核心用户故事

> 我正在开会，来不及打字。按住快捷键说了30秒，松开后左边显示我说的话（带语气词的原话），右边已经流式输出了一段干净利落的会议纪要——语气词没了，段落分好了，决策项和待办事项自动标了出来。

## 功能模块

### 1. 语音采集

- **按住说话**：按住空格键或屏幕按钮开始录音，松开停止
- **实时波形**：录音时显示音频波形动画，给用户"我在听"的反馈
- **时长计时**：录音计时器显示
- **权限处理**：首次使用请求麦克风权限，被拒时给出明确引导

### 2. 语音识别（STT）

- **引擎**：浏览器 Web Speech API
  - 零延迟（浏览器本地引擎），支持实时 interim results
  - Chrome 对中文 SpeechRecognition 支持最完善
  - 免费，无 API 调用成本
- **原文输出**：说话过程中逐句流式显示在左栏（原文区）
- **语言**：中文为主，支持中英混合

选型理由：Web Speech API 的首字延迟约 50ms（本地处理），对比云端 API（DeepSeek V4 音频输入需 800-1500ms 网络往返+推理），实时体验差距显著。DeepSeek V4 的核心价值在改写环节——它理解语义后能自动修正大部分 STT 小错误，比单纯追求识别率性价比更高。

### 3. AI语义改写（核心）

**两级处理策略**：

```
说话中 ──→ 检测到句边界（。！？/ 停顿>800ms）
  │            │
  │            ▼
  │        逐句改写（DeepSeek V4 flash, stream:true）
  │            │
  │            ▼
  │        右栏流式追加显示
  │
  ▼
松开按钮 ──→ 全局润色（DeepSeek V4 flash, 全文上下文）
               │
               ▼
            最终格式化输出
```

- **逐句改写**（utterance-level）：检测到标点或长停顿，立即发送该句给 DeepSeek V4。用 `stream: true` 让改写结果逐字流回前端，追加到右栏。适用于短句场景和长场景的"边出边看"需求。
- **全局润色**（final pass）：松开按钮后，将全文+场景参数一次性发送，做段落统一、去重衔接、格式对齐。仅会议场景追加说话人标注。

改写能力：
- 去除语气词（嗯、那个、就是、然后、反正、这个嘛……）
- 修正口语化断句和表述重复
- 自动添加标点符号和段落分隔
- 场景匹配的语气和输出格式（见场景预设）

### 4. 场景预设

| 场景 | 改写行为 | 典型用途 |
|------|---------|---------|
| 通用 | 去语气词 + 标点 + 分段，保留原意和语气 | 默认模式，日常输入 |
| 商务邮件 | 正式语气，自动加称呼和落款，段落分明 | 口述工作邮件 |
| 即时聊天 | 去除语气词但保留口语感和短句节奏 | 微信、钉钉回消息 |
| 会议纪要 | 提取决策项 + 待办事项 + 负责人，结构化输出；尝试区分发言人 | 会后口述整理 |
| 代码注释 | 保留技术术语精度，输出 JSDoc / docstring 格式 | 口述函数和类型注释 |

### 5. 双栏展示

- **左栏（原文）**：说话过程中实时流式显示 Web Speech API 原始转录
- **右栏（改写）**：逐句改写结果流式追加 + 全局润色后最终更新
- 左右对比，一眼看清"改前 vs 改后"
- 全局润色完成后右栏轻微高亮闪烁，提示"改写完成"

### 6. 噪音和多人场景处理

**噪音**：
- STT 层：Web Speech API 自带基础降噪，持续噪音下置信度下降会让识别自然中断
- 改写层：prompt 中追加指令——"如果原文包含明显噪音导致的乱码或无意义字符，忽略并基于可理解部分输出"
- 覆盖办公室常见环境噪音（键盘、空调、远处人声），嘈杂环境不在当前范围

**多人对话**：
- 不做声纹识别（speaker diarization），仅会议纪要场景下，在全局润色 prompt 中追加指令让模型尝试从文本层面推断说话人切换
- 改写结果中标记为"发言A / 发言B"，不做身份匹配

### 7. 基础功能

- 一键复制改写结果
- 历史记录（最近5条，存 localStorage）
- 改写结果字数统计

---

## 技术架构

```
┌───────────────────────────────────────────────────┐
│                 FlowTalk Web App                   │
├───────────────────────────────────────────────────┤
│  Browser (React 18 + TypeScript + Vite)            │
│  ┌───────────┐  ┌───────────┐  ┌───────────────┐ │
│  │AudioCapture│  │ DualPane  │  │ SceneSelector │ │
│  │(Web Speech│  │(原文|改写) │  │  (5种场景)    │ │
│  │ API)      │  │          │  │              │ │
│  └─────┬─────┘  └───────────┘  └───────────────┘ │
│        │                                           │
│  ┌─────┴─────────────────────────────────────┐    │
│  │          Socket.IO Client                 │    │
│  └──────────────────┬───────────────────────┘    │
└─────────────────────┼─────────────────────────────┘
                      │ WebSocket
┌─────────────────────┼─────────────────────────────┐
│  Server (Node.js + Express + TypeScript)           │
│  ┌──────────────────┴───────────────────────┐     │
│  │          Socket.IO Server                │     │
│  └──────────────────┬───────────────────────┘     │
│                     │                              │
│  ┌──────────────────┴───────────────────────┐     │
│  │         Rewrite Pipeline                 │     │
│  │  ┌──────────────┐  ┌─────────────────┐  │     │
│  │  │Sentence-level│  │  Final Global   │  │     │
│  │  │  Rewrite     │  │  Polish         │  │     │
│  │  │(per sentence,│  │(full context,   │  │     │
│  │  │stream:true)  │  │stream:true)     │  │     │
│  │  └──────┬───────┘  └────────┬────────┘  │     │
│  │         └───────────────────┘           │     │
│  │         DeepSeek V4 API (flash)          │     │
│  └──────────────────────────────────────────┘     │
└────────────────────────────────────────────────────┘
```

- **前端**：React 18 + TypeScript + Vite + Ant Design 5.x + Tailwind CSS + Socket.IO Client
- **后端**：Node.js + Express + TypeScript + Socket.IO + DeepSeek V4 API（OpenAI 兼容接口）
- **存储**：localStorage（历史记录），无数据库

---

## 非功能需求

### 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 语音识别首字延迟 | < 500ms | Web Speech API 本地处理 |
| 逐句改写首字延迟 | < 1s | DeepSeek V4 flash + streaming |
| 单句改写完整耗时 | < 2s | 典型10-30字句子 |
| 全局润色完整耗时 | < 3s | 典型30秒语音输入的全文 |

### 兼容性

- Chrome 90+（主力支持，Web Speech API 中文识别最完善）
- Edge 90+（兼容支持）
- 桌面端优先，移动端作为后续方向

### 错误处理

- 麦克风权限被拒：明确提示 + 引导用户修改浏览器设置
- DeepSeek V4 API 超时/失败（含逐句改写和全局润色）：右栏降级显示原始转录，提供重试按钮
- WebSocket 断开：自动重连 + 录音本地暂存

---

## 3日 PR 拆解

```
Day 1（5/23）— 骨架 + 音频 + 识别
  PR #1: 项目脚手架（前后端基础架构）
  PR #2: 音频采集 + Web Speech API STT
  PR #3: WebSocket 实时连接 + 基础双栏布局

Day 2（5/24）— AI改写核心
  PR #4: 基础改写管线（通用模式，逐句+全局两级）
  PR #5: 场景预设系统（5种场景 prompt 模板）
  PR #6: 流式改写 UI（打字机效果 + 双栏实时更新）

Day 3（5/25）— 打磨 + 交付
  PR #7: UI 打磨（动画、状态、错误处理、空状态）
  PR #8: 辅助功能（历史记录、复制、字数统计）
  PR #9: README + Demo 视频
```

---

## 验收标准

- [ ] 按住空格/按钮 → 开始录音，显示波形动画，松开 → 停止
- [ ] 说话过程中左栏实时流式显示原始转录
- [ ] 逐句改写结果在右栏流式追加显示
- [ ] 松开按钮后全局润色完成，右栏最终更新
- [ ] 5种场景切换，输出风格和格式有明显差异
- [ ] 双栏对比清晰可用
- [ ] API 失败时降级显示原文 + 重试按钮，不崩溃
- [ ] Chrome 桌面端完整可跑通全流程
