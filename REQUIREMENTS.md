# FlowTalk — 产品需求文档

## 产品定位

不只是语音转文字，更是"你说人话，它出成文稿"的AI输入工具。

**核心差异化**：AI语义改写层。现有产品（搜狗、讯飞、豆包、微信输入法）只做语音识别这一步，不做AI改写。用户拿到的原始转录充满语气词、重复和断句混乱，手动修改的时间有时比直接打字还长。FlowTalk 在识别之后加了一层 AI 改写——去噪、润色、分段、场景匹配格式，让语音输入直接产出"可用的文字"。

**场景边界**：FlowTalk 的核心场景是单人语音输入。会议模式定位为"一个人会后口述整理纪要"，而非替代飞书妙记、通义听悟这类多人会议转录工具。

## 核心用户故事

> 我正在开会，来不及打字。按住快捷键说了30秒，松开后左边显示我原话，右边已经流式输出了一段干净利落的会议纪要——语气词没了，段落分好了，决策项和待办事项自动标了出来。我还可以再按住继续说，内容追加不覆盖。

## 功能模块

### 1. 语音采集

- **按住说话**：按住空格键（键盘模式）或屏幕录音按钮（鼠标模式），松开停止
- **录音状态指示**：录音时显示 CSS 呼吸动画（pulsing ring），替代真实波形。参考 Apple 听写和 Google 语音搜索的做法——给出"我在听"的视觉反馈即可，不额外拉音频流
- **时长计时**：录音计时器显示
- **权限处理**：首次使用请求麦克风权限，被拒时给出引导说明
- **追加模式（默认）**：松开后左栏原文不清空。再次按住说话，新识别内容追加到左栏末尾（自动加空行分隔两次录音段）。每次松开自动触发改写，以左栏当前全部原文作为输入。用户可手动清空重新开始

设计理由：Web Speech API 不提供原始音频流，无法获取音量数据做真实波形。同时开两个麦克风流（一个给波形一个给识别）技术上可行但不值得——引入 AudioContext 节点管理和 RAF 循环渲染，对核心价值贡献不大。

### 2. 语音识别（STT）

- **引擎**：浏览器 Web Speech API（Chrome SpeechRecognition）
  - 零延迟（浏览器本地引擎），支持 interim results 和 isFinal 事件
  - Chrome 对中文 SpeechRecognition 支持最完善
  - 免费，无 API 调用成本
- **语言**：中文为主，支持中英混合
- **左栏展示策略**：
  - 正文区：只显示 `isFinal=true` 的完整句子，稳定不闪烁
  - 临时提示区（左栏顶部半透明区域）：显示当前 `interim` 临时识别文字（灰色、斜体），变成 final 后移除，追加到正文区

选型理由：Web Speech API 首字延迟约 50ms（本地处理），对比云端 STT（DeepSeek V4 音频输入需 800-1500ms 网络往返+推理），实时体验差距显著。DeepSeek V4 的核心价值在改写环节——它理解语义后能自动修正大部分 STT 的小错误，比单纯追求识别率性价比更高。

### 3. AI语义改写（核心）

**两级处理策略**：

```
Web Speech API 触发 isFinal=true
    │
    ▼
该句追加到左栏正文
    │
    ▼
HTTP POST /api/rewrite/sentence
  { sentence, scene, accumulated_context }
    │
    ▼
DeepSeek V4 flash, stream:true
    │
    ▼
SSE 逐字返回 → 右栏流式追加显示
    │
    │  (多句累计)
    ▼
用户松开按钮
    │
    ▼
HTTP POST /api/rewrite/polish
  { full_raw_text, scene }
    │
    ▼
DeepSeek V4 flash, stream:true
    │
    ▼
SSE 逐字返回 → 右栏最终替换为润色后结果
    │
    ▼
右栏轻微闪烁提示"润色完成"
```

- **逐句改写**（sentence-level）：浏览器 `isFinal` 事件触发。每句独立发送给 DeepSeek V4，附带已累积的上下文，帮助模型理解当前句在全文中的位置。`stream: true` 让改写结果逐字流回，追加到右栏。
- **全局润色**（final polish）：松开按钮后，将全文 + 场景参数一次性发送。负责段落统一、去重衔接、格式对齐，产出最终版本替换右栏内容。

改写能力：
- 去除语气词（嗯、那个、就是、然后、反正、这个嘛、怎么说呢……）
- 修正口语化断句和表述重复
- 自动添加标点符号和段落分隔
- 场景匹配的语气和输出格式

**句边界判断**：使用 Web Speech API 原生的 `isFinal` 事件，不做前端自定义停顿检测。浏览器引擎已经做了成熟的 VAD（Voice Activity Detection），比我们手动写 800ms 计时器更可靠且零维护成本。

**传输协议**：HTTP + SSE（Server-Sent Events）。两个 POST endpoint 各自返回 SSE 流。不引入 Socket.IO，原因是逐句改写是单向请求-流式响应模式，不需要全双工，且 HTTP SSE 的状态管理更简单——每个请求独立，不需要服务端维护会话。

### 4. 场景预设

| 场景 | Prompt 行为 | 输出格式 | 典型用途 |
|------|-----------|---------|---------|
| 通用 | 去语气词 + 标点 + 分段，保留原意 | 自然段落 | 日常输入 |
| 商务邮件 | 正式语气，自动加称呼/落款/主题行 | 邮件格式 | 口述工作邮件 |
| 即时聊天 | 去除语气词但保留口语感和短句节奏 | 自然短句 | 微信、钉钉 |
| 会议纪要 | 提取决策项 + 待办事项 + 时间节点，结构化输出 | 分条纪要 | 会后整理 |
| 代码注释 | 保留技术术语精度，按 JSDoc 模板输出 | JSDoc 块 | 口述函数/类型注释 |

**代码注释模式的格式规范**：

Prompt 中嵌入明确的输出模板而非让模型自行决定：

```
场景：代码注释
规则：
1. 保留用户口述中的技术术语不做改写（如 Promise、userId、async）
2. 如果口述中没有明确类型，根据变量名合理推断，标注在类型中
3. 如果口述信息不足以生成完整 JSDoc，只生成已知字段，不要编造
4. 输出格式：
/**
 * <一句话功能描述>
 * @param {类型} 参数名 - <参数说明>
 * @returns {类型} <返回值说明>
 */

输入示例："这个函数叫 getUserName，参数是 userId 字符串，返回用户名或者 null"
输出示例：
/**
 * 根据用户ID获取用户名
 * @param {string} userId - 用户ID
 * @returns {string|null} 用户名，如果不存在则返回null
 */
```

前端在代码注释场景下右栏使用等宽字体渲染，并额外显示"复制代码"按钮（只复制注释文本，不复制 markdown 包裹）。

**会议纪要模式**：聚焦在提取决策项、待办事项、时间节点和结构化输出，不做说话人分离（speaker diarization）。没有声纹识别做底，纯靠 prompt 推断"发言A/发言B"不可靠，会产生幻觉损害产品可信度。

### 5. 噪音处理

两层的轻量处理，不引入额外模块：

- **STT 层**：Web Speech API 自带降噪，持续噪音下置信度下降会导致识别自然中断
- **改写层**：prompt 中追加——"如果原文包含明显噪音导致的乱码或无意义字符，忽略并基于可理解部分输出"

覆盖办公室常见环境噪音（键盘、空调、远处人声），嘈杂环境（工地、地铁）不在当前产品范围。

### 6. 双栏展示

- **左栏（原文）**：`isFinal` 的完整句子逐句追加。临时识别文字在独立的灰色半透明提示区显示
- **右栏（改写）**：逐句改写结果流式追加 → 松开后全局润色最终替换
- **追加模式分段**：每次松开再按，左栏自动插入空行分隔不同录音段，帮助用户分辨哪些内容是哪一次说的
- **完成反馈**：全局润色完成后右栏轻微闪烁，提示"改写完成"

### 7. 历史记录

- 存储：localStorage，最近 20 条
- 每条记录保存：`{ id, raw, polished, scene, timestamp, duration }`
- **回填功能**：点击历史记录项，将原文和改写结果分别恢复到左右栏。用户可以继续编辑、追加录音或切换场景重新润色
- 列表按时间倒序，显示场景标签、字数、录音时长

### 8. 基础操作

- 一键复制改写结果（代码注释场景额外提供"复制代码"按钮）
- 清空按钮：手动重置左右栏，开始全新输入
- 字数统计（改写结果）
- 场景切换：录音前/录音后均可切换，切换后自动重新触发改写

---

## 技术架构

```
┌───────────────────────────────────────────────────┐
│                 FlowTalk Web App                   │
├───────────────────────────────────────────────────┤
│  Browser (React 18 + TypeScript + Vite)            │
│  ┌───────────┐  ┌───────────┐  ┌───────────────┐ │
│  │AudioCapture│  │ DualPane  │  │ SceneSelector │ │
│  │(Web Speech│  │(原文|改写) │  │  (5种场景)    │ │
│  │ API)      │  │          │  │              │ │
│  └─────┬─────┘  └───────────┘  └───────────────┘ │
│        │  isFinal                                   │
│  ┌─────┴─────────────────────────────────────┐    │
│  │  fetch() + ReadableStream (SSE consumer)   │    │
│  └──────────────────┬───────────────────────┘    │
└─────────────────────┼─────────────────────────────┘
                      │ HTTP POST + SSE response
┌─────────────────────┼─────────────────────────────┐
│  Server (Node.js + Express + TypeScript)           │
│                                                     │
│  POST /api/rewrite/sentence  (逐句改写)             │
│  POST /api/rewrite/polish    (全局润色)             │
│           │                                         │
│           ▼                                         │
│  DeepSeek V4 API (deepseek-v4-flash)                │
│  OpenAI-compatible, stream:true                     │
└────────────────────────────────────────────────────┘
```

- **前端**：React 18 + TypeScript + Vite + Ant Design 5.x + Tailwind CSS。SSE 消费使用原生 `fetch()` + `ReadableStream`，不引入额外库
- **后端**：Node.js + Express + TypeScript + DeepSeek V4 API（OpenAI 兼容接口）
- **存储**：localStorage（历史记录），无数据库

---

## 非功能需求

### 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 语音识别首字延迟 | < 500ms | Web Speech API 本地处理 |
| 逐句改写首字延迟 | < 1s | DeepSeek V4 flash + streaming |
| 单句改写完整耗时 | < 2s | 典型10-30字句子 |
| 全局润色完整耗时 | < 5s | 典型30秒语音的全文（~150字）|

### 兼容性

- Chrome 90+（主力支持，Web Speech API 中文识别最完善）
- Edge 90+（兼容支持）
- 桌面端优先

### 错误处理

- 麦克风权限被拒：明确提示 + 引导用户修改浏览器设置
- 逐句改写失败：该句保留原文显示在右栏，不影响后续句子处理
- 全局润色失败：显示重试按钮 + 右栏保留逐句改写的累积结果（不丢失）
- 网络断开：识别不受影响（本地处理），改写请求失败时有明确状态提示

---

## 3日 PR 拆解

```
Day 1（5/23）— 骨架 + 音频 + 识别
  PR #1: 项目脚手架（前后端基础架构）
  PR #2: 音频采集 + Web Speech API STT
  PR #3: 改写 API + 前端 SSE 消费 + 基础双栏布局

Day 2（5/24）— 场景系统 + 完整交互
  PR #4: 场景预设系统（5种 prompt 模板 + 场景切换）
  PR #5: 追加模式 + 历史记录 + 回填
  PR #6: 流式改写 UI（打字机效果 + 双栏实时更新 + 代码注释等宽字体）

Day 3（5/25）— 打磨 + 交付
  PR #7: UI 打磨（动画、状态、错误处理、空状态、响应式）
  PR #8: README + Demo 视频 + 发布
```

---

## 验收标准

- [ ] 按住空格/按钮 → 开始录音，显示呼吸动画，松开 → 停止
- [ ] 说话过程中左栏正文追加 isFinal 句子，临时提示区显示 interim 文字
- [ ] 逐句改写结果在右栏流式追加显示（打字机效果）
- [ ] 松开按钮后全局润色完成，右栏最终更新并闪烁提示
- [ ] 追加模式：松开后再按，左栏不清空，内容追加
- [ ] 5种场景切换，输出风格和格式有明显差异
- [ ] 历史记录列表 + 点击回填到左右栏
- [ ] 代码注释场景：等宽字体 + "复制代码"按钮
- [ ] API 失败时降级处理，不崩溃，不丢已有结果
- [ ] Chrome 桌面端完整可跑通全流程
